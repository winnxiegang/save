<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/package_item"
    android:layout_width="120dp"
    android:layout_height="160dp"
    android:background="@drawable/bg_package_normal"
    android:orientation="vertical">

    <TextView
        android:id="@+id/package_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="15dp"
        android:layout_marginTop="15dp"
        android:drawableLeft="@drawable/ic_package_time"
        android:text="12小时"
        android:textColor="@color/white"
        android:textSize="@dimen/sp10" />

    <TextView
        android:id="@+id/package_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_marginRight="15dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/ic_package_tag"
        android:gravity="center"
        android:padding="5dp"
        android:text="X5"
        android:textColor="@color/white"
        android:textSize="13sp" />

    <ImageView
        android:id="@+id/package_iv"
        android:layout_width="55dp"
        android:layout_height="55dp"
        android:layout_centerInParent="true" />

    <TextView
        android:id="@+id/package_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        android:gravity="center"
        android:text="必胜"
        android:textColor="@color/white" />
</RelativeLayout>